Docker swarm SECRETS (no extraíbles — valor write-only en el raft):
- VPS1   : tunnel_ssh_key  -> clave SSH privada del container autossh (db_tunnel). Recrear desde la keypair del túnel.
- Orq.   : model_hub_token              -> token HuggingFace para descargar el modelo en vLLM. Recrear desde cuenta HF.
Docker swarm CONFIGS (extraídos a ficheros):
- VPS1   : consultor_db_init_v2  -> consultor_db_init_v2.sql (init de postgres).
